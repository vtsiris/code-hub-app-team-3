import { PriorityPipePipe } from './priority-pipe.pipe';

describe('PriorityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
